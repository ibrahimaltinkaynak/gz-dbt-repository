version: 2

sources:
  - name: raw # Le nom du dataset BigQuery
    schema: gz_raw_data
    tables: 
      - name: sales # La première table
        identifier: raw_gz_sales
      - name: product # La deuxième table
        identifier: raw_gz_product
      - name: ship # La troisième table
        identifier: raw_gz_ship
